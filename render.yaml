services:
  - type: web
    name: cyberai-frontend
    env: static
    buildCommand: "npm install -g pnpm && pnpm install && pnpm build"
    staticPublishPath: "./dist"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
